# HANDOFF 2026-06-06 — 過去問の「授業回別」分類プロジェクト

## 目的
過去問648問（`hygiene_with_images_v4.jsonl`）を、松川先生の授業回（全45回＝3科目×15回）ごとに分類し、学習アプリ（`index.html`）で「授業回」フィルタとして使えるようにする。学生が「今日の授業範囲の過去問」だけで演習できるのが狙い。

## 全体設計（確定済み）
- 全45回 = 3科目 × 15回。各科目は新コアカリE領域に対応：
  - **衛生化学I**（E-2 栄養・食品）… 過去問 **176問**
  - **衛生化学II**（E-3 化学物質・環境）… 過去問 **206問**
  - **公衆衛生学**（E-1 保健・疫学・感染症）… 過去問 **266問**
  - 合計648問、**漏れ0**で3科目に振り分け済み。
- 各回のテーマ・キーワードは `syllabus_3kamoku_45kai.md` を参照（設計図）。

## 仕組み（実装済み・本番反映済み）
- **`lecture_map.json`** … 授業回→過去問IDの対応表。
  形式：`{ "衛II": { "label","icon","color", "lectures": { "9": { "theme", "ids":[...] } } } }`
- **`index.html`** … 授業回フィルタを実装済み（commit `3c721bc`・`942a2ae` で本番反映）：
  - 状態変数 `selectedLecture`, `LECTURE_MAP`
  - `loadData` で `lecture_map.json` を fetch
  - `getPool` に授業回フィルタ（`selectedLecture="衛II-9"` → lecture_mapの該当 ids で絞る）
  - `renderLectureFilters()` で科目別ボタン描画（`renderHome` から呼ぶ）
  - HTML：「🔍 詳細フィルタ」内の「📅 国試の回・授業回で選ぶ」の中、**国試の回の下に授業回**
  - ボタン表記：「**衛II-9 地球環境・生態 (17)**」（略記-回 テーマ (問題数)）

## 進捗
- ✅ **衛II-9（地球環境・生態）17問** 本番反映済み（commit `942a2ae`）
- ✅ **衛II-10（廃棄物）16問** 本番反映済み（commit `2bb5187`）
- ⬜ 衛II 第1〜7・11〜15回（第8回は中間試験で割り当てなし）
- ⬜ 衛I 全15回（第8回・試験除く）
- ⬜ 公衆衛生学 全15回（第15回・まとめ除く）
- 🔜 **次回は衛II-11（放射線・電磁波）**。過去問「放射線」分野23問が中心。紫外線・電磁波が他ラベル（大気環境等）に隠れていないか内容で確認する（第9・10回と同じ救済方式）。

## 重要な学び（必読）
1. **過去問の `_subcat`/`new_corecurri_code` は97%が機械推定**（`subcat_inferred`）で、内容と乖離した誤分類が混じる。授業回に振り分ける際、**シラバスのキーワードで“内容から”拾い直す**と、別ラベルに隠れた該当問題を救済できる。
   - 第9回の実例：17問中8問が別ラベルから救済 → `100-135`(産業保健), `110-137`(遺伝子組換え), `097-137`/`099-136`/`103-136`/`106-137`/`108-22`/`101-134`(いずれも有害化学物質ラベルだが実体は地球環境・生態)。
   - **必ず `problem_text`＋`choices`＋`comment` の全文を対象に検索すること**（_subcatだけ見ると取りこぼす）。
2. **リスクコミュニケーションは過去問に0問**（新しい概念）。新カリ対応は予想問題（`future_questions.jsonl`）or 新規作成で別途。
3. **1問が複数の授業回に属してOK**（idsに重複登録可）。境界問題（例：地球環境の大気・廃棄物・紫外線）は、関連する各回に出してよい。第9回では大気/廃棄物/紫外線/水は他の回に譲った。

## 次回の作業手順（1回ぶん）
1. `syllabus_3kamoku_45kai.md` で対象回のテーマ・キーワードを確認。
2. キーワードで過去問を抽出（`problem_text`＋`choices`＋`comment` を対象に。_subcatラベルに頼らない）。
3. 一問ずつ吟味し、その回に該当する `problem_id` を確定（他ラベルからの救済も。境界は他の回と要相談）。
4. `lecture_map.json` の該当回に追記（`科目キー → lectures → "回番号" → {theme, ids}`）。新科目なら `label`/`icon`/`color` も追加。
5. 動作確認：`preview_start "eisei"`（`.claude/launch.json`、python http.server 8765）→ `preview_eval` で `selectLecture('衛X-N')` 後に `getPool().length` を確認。
6. `git add index.html lecture_map.json && git commit && git push`。
7. このHANDOFFと syllabus の進捗欄を更新。

## 関連ファイル
- `lecture_map.json` … 対応表（**編集対象**）
- `index.html` … アプリ本体（授業回フィルタ実装済み。基本もう触らない）
- `syllabus_3kamoku_45kai.md` … 3科目45回シラバス（設計図）
- `hygiene_with_images_v4.jsonl` … 過去問648問（problem_id例：`097016`＝97回16問）
- バックアップ：`index.html.bak_pre_lecture_*`（.gitignoreで除外）

## ユーザーについて
日本語で話す非エンジニアの薬学部教員。手順は一つずつ丁寧に。専門用語は避ける。勝手に大きく進めず、節目で確認する。
