# HANDOFF 2026-06-09 — 学習アプリに「選択肢シャッフル」を実装

※この回は過去問分類ではなく **学習アプリ(index.html)の機能追加**。分類プロジェクトの最新は **HANDOFF_2026-06-07.md** を参照。

## 今日やったこと（2026-06-09）

1. **git pull**：ローカルが `origin/main` より36コミット遅れていたので最新へ追従（`9d66e1a`→`f9ee0a1`）。
2. **テストで選択肢をシャッフルする機能（方式A）を実装**してコミット・プッシュ（`8fe2cce`）。

## 機能の概要（方式A）
出題ごとに選択肢の**並び順をランダム化**し、**1〜5の番号も表示順に振り直す**。これに合わせて**正解番号**と**解説（`1：…2：…`形式）の番号**も自動で整合させる。狙いは「学生が場所（位置）で答えを暗記するのを防ぐ」こと。

- **データ(jsonl)は一切変更していない**。表示時のみ `index.html` のJSで並べ替える（オフにすれば即元通り）。
- ホーム画面に **「🔀 選択肢をシャッフル」トグル**（既定オン、`localStorage['eisei_shuffle']` に記憶）。検証・確認時はオフにできる。
- **閲覧モードは番号順のまま**（学習用に見やすく）。テスト機能のみシャッフル。

### 自動で除外する問題（シャッフルしない）
| 対象 | 概数 | 理由 |
|---|---|---|
| 図で選ぶ問題（`choices[0]==='IMAGES'`） | 82 | 選択肢が画像で位置固定 |
| 数値・年代の順序（昇降順） | 約99 | 順序に意味がある |
| 「上記/下記/いずれも/1と2」等 | 約24 | 位置・相互参照に依存 |
| 「解なし/いずれか正解」等の非数字answer | 3 | 番号を動かすと壊れる（097132, 101021, 103127） |
| 「選択肢1は…」と番号で言及する解説 | 約22 | 並べ替えると解説とズレる（hygiene側） |

→ 残り **約1,800問でシャッフル有効**（future約1,323／hygiene約510）。

## 実装場所（index.html・保守用メモ）
- 状態変数: `shuffleChoices`（既定オン）, `currentRender`（現在問題の並べ替え情報）
- 中核関数（`renderQuestion` の直前にまとめて追加）:
  - `canShuffle(q)` … 除外判定（IMAGES／非数字answer／POS_REF_RE／`isOrderedNumeric`）
  - `isOrderedNumeric(ch)` / `leadingNumber(c)` … 数値・年代順の検出
  - `parseSegs` / `remapComment` … 解説の `N：` を表示順に振り直す（`1..n`が揃う時のみ）
  - `commentClass(comment,n)` … `full`=番号式(振り直す) / `ref`=番号言及あり(並べ替え不可) / `plain`,`none`=安全
  - `buildRender(q)` … 表示順(order)・正解番号(answerNums)・解説(comment)を組み立て（元データ不変）
- 描画/判定の変更点:
  - `renderQuestion`: 先頭で `currentRender = buildRender(q)`。テキスト選択肢を `currentRender.order` 順に描画（番号は表示位置で振り直し）
  - `processAnswer`: 正解判定は `currentRender.answerNums`、解説表示は `currentRender.comment` を使用
  - `renderShuffleToggle()` / `setShuffleChoices()` をホーム描画に追加
- トグルのCSS: `.shuffle-checkbox`（`.weak-checkbox` を踏襲、色は青系）

## 検証（実施済み）
- JS構文チェック：OK
- **全1,969問（テキスト選択肢）で不変条件パス・失敗0**：並べ替え後の正解位置の中身＝元の正解、解説の番号対応も完全一致。図問題82件は誤並べ替えゼロ。
- 実ブラウザ（python http.server + プレビュー）で：単一選択（F001：正解2→4へ移動、緑判定・解説「4：正。…」一致）／複数選択（097121）／数値順の除外（097122）／トグルOFF復元 — すべて正常。

## 残る注意点 / 今後の調整余地
- 「順序に意味がある」の自動判定は機械的。**シャッフルすべきでない問題**を見つけたら個別除外を追加できる（`canShuffle` に条件追加、または問題側に除外フラグを持たせる運用も可）。
- シャッフル中の不具合報告は「選択肢3」が並べ替え後の番号を指す（管理画面=admin.htmlは元の順なので照合に一手間）。気になればテスト時オフ運用も可。
- 方式A（番号も振り直し）を採用。番号での暗記も防げるが、その分「解説の番号合わせ」が必須。番号言及の解説（ref）は安全側で除外している。

## 関連ファイル
- `index.html`（変更対象。`8fe2cce` でコミット済み）
- バックアップ: `index.html.bak_pre_shuffle_20260609_121936`（`.gitignore`対象＝コミットされない。ローカルにのみ保管）
