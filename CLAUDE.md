# eisei_yakugaku — 衛生薬学 国試対策・予想問題プロジェクト

このプロジェクトで作業するClaude/AIアシスタントは、まずこのファイルを読んでから作業を開始してください。

## このプロジェクトは何？

順天堂大学薬学部 衛生化学分野の予想問題・過去問データベース、および学生向け学習Webアプリ。

- `future_questions.jsonl` — 予想問題 1,413問（F001〜）
- `hygiene_with_images_v4.jsonl` — 過去問 648問（97〜111回）
- `index.html` — 学生向け学習アプリ（テスト/閲覧/検索/進捗）
- `admin.html` — 管理画面（学生フィードバック・検証履歴）
- `by_subcat_md/` — 分野別 Markdown エクスポート（49ファイル）
- `新コアカリ分類案.md` — 令和4年度改訂コアカリの分類体系・旧→新マッピング
- 学生フィードバックは Supabase に蓄積（`question_feedback` / `question_verifications`）

## 作業相手について

このリポジトリの所有者は、**日本語で話す非エンジニアの薬学部教員**です。

- 専門用語は避け、必要なら身近なたとえで、手順は一つずつ丁寧に説明する
- 「自分でやってみて」と放り出さず、一緒に進める姿勢で
- 他のAIに同じ作業を頼んで、途中まで（誤った）変更を加えていることがある。説明された意図を鵜呑みにせず、実際のファイルの状態を確認する
- ファイル操作・解凍・形式変換などは苦手

---

## 問題作成のガイドライン

### 攪乱肢（誤答選択肢）の作り方

- **絶対化表現を攪乱肢に使わない**（学生が「絶対化＝怪しい」と学習してしまい、知識ゼロでも消去できてしまう）
  - **量的絶対化**: 「のみ」「だけ」「全く」「ない」「必ず」「絶対」「すべて」「あらゆる」
  - **時間的絶対化**: 「直ちに」「ただちに」「即座に」「常に」「決して」「最優先で」
  - **当為的絶対化**: 「〜しなければならない」「〜すべきではない」「〜する義務がある」（文脈で許容可）
  - 絶対化を使うのは正答側のみ（必要な場合）
  - 代わりに「主軸である」「副次的とされる」「認められていない」「優先される」「先行させる」など、相対化・条件付き表現を使う
- 攪乱肢が正答と同程度の長さになるよう調整する（長さで正答推定されないため）
- 攪乱肢は「中途半端な知識だと迷う」内容に。雑な逆方向表現は避ける
- 全選択肢の文末スタイルを揃える（「〜である」「〜という」「〜する」が混在しない）

### 正答位置の分散

予想問題1,413問の現状分布（要改善）:
- **1つ選べ問題**: pos 1=15.7%, pos 2=24.2%, pos 3=24.1%, pos 4=20.8%, pos 5=15.2%
  - pos 1, 5 が枯渇気味。新規・改修時は意識的に pos 1, 5 を増やす
- **複数選べ問題**: pos 1=39.1%, pos 2=27.1%, pos 3=16.1%, pos 4=9.7%, pos 5=7.9%
  - pos 4, 5 が極端に枯渇。改修時は pos 4, 5 を含む正答に動かす
- ただし「年代順」「数値の昇降順」など順序に意味がある選択肢は動かさない

### new_corecurri_code の確認

リスクコミュニケーション系の問題は `E-3-1-(1)` ではなく、以下から選ぶ：
- `E-3-1-(8)` 化学物質リスクコミュニケーション
- `E-3-2-(7)` 環境リスクコミュニケーション（環境省定義など）
- `E-2-2-(10)` 食品安全性リスクコミュニケーション
- `E-1-1-(8)` 環境要因リスクコミュニケーション
- `E-1-2-(9)` 感染症リスクコミュニケーション

詳細マッピングは `新コアカリ分類案.md` を参照。

### 過去の品質改善事例（参考）

- F586型「設定されていない」反復 → 具体的数値誤答へ
- F261「栄養ア」謎テキスト残骸、F163「曝気」答えバレラベル等の図問題
- 選択肢長さアンバランス（正答だけ長い）
- 詳細は `検証経過まとめ.md`, `HANDOFF_2026-04-10.md` 参照

### 数値・年代の妥当性チェック（必須）

問題内の数値（特に年代・西暦）は、**現在の年（システム日付）と矛盾しないか**を必ず確認する。

具体例（バッチ3で発見）:
- F1017 選択肢3「京都議定書は**2030年**に採択された」← 現在2026年なのに未来の採択年を主張。エージェント5係全員が見落とし、ユーザーが発見。
- 実際の京都議定書採択年は1997年。

チェックポイント:
- 過去の出来事として書かれている年が現在より未来になっていないか
- 「採択年」「発効年」「制定年」「施行年」「宣言年」等の年表事項
- 「○○以降」「○○まで」のような時系列表現が現在と整合するか
- 「制度開始」「目標年」が実情と合っているか

---

## AIエージェントによる問題ブラッシュアップの流れ

複数エージェント並列レビュー方式（2026年5月確立、5月末に3段階自動化を追加）：

1. **対象選定** — 講義回・分野別に10〜20問単位で
2. **バックアップ** — `future_questions.jsonl.bak_pre_aireview_*` を取得
3. **作業ファイル抽出** — `_work/lecture<N>_batch<M>.jsonl`
4. **7係並列レビュー**：
   - コアカリ係（`new_corecurri_code` 妥当性、SBO適合）
   - 過去問係（国試風味、過去問との重複・差別化）
   - 衛生薬学事実係（ハルシネーション・最新情報）
   - 品質レビュー係（攪乱肢の質、日本語、正答位置の浮き）
   - 選択肢ばらし係（正解位置の偏り — クロ姉さん本体が担当）
   - 画像整合性係（画像あり問題のみ）
   - 新コアカリ対応係（2024年制度改正の反映、コード整合）
5. **所見集約＋3段階分類**（次項参照）
6. **3段階の処理を実行**：
   - **Tier 1**: 自動反映 → 事後レポート（ユーザー入力なし）
   - **Tier 2**: 一括承認形式で確認（"全部OK / 番号で却下"）
   - **Tier 3**: 1問ずつ個別判断（`AskUserQuestion`）
7. **承認分のみ反映** — `decisions_batch<M>.json` に蓄積後、`apply_decisions.py` で jsonl 更新
8. **進捗ログ更新** — `ai_review_log_第<N>回.md` のチェック表に反映

## 3段階の自動化分類基準

ユーザー負荷を減らすため、変更内容を3段階に分類して処理する：

### Tier 1 — 自動反映（ユーザー確認なし、事後レポートのみ）

姉さん／AIが**ユーザー確認なしに反映**する範囲。条件を満たすもののみ：

| 種類 | 条件 |
|------|------|
| 解説への追記 | 衛生薬学事実係が「事実誤認なし」を確認済み、かつ追加内容のみ（既存記述と矛盾しない）、かつ長さ妥当（200字以内程度） |
| `new_corecurri_code` の修正 | コアカリ係＋新コアカリ対応係の**両方が同じ推奨コード**を出している、または `新コアカリ分類案.md` のマッピング表で明確に判定可能（例: リスコミ系の `E-3-1-(1)` → 内容に応じた正しいコード） |
| 単純な日本語修正 | 主語の補い、文末スタイルの統一、誤字脱字の修正など、意味を変えない調整 |

これらは「機械的判定が可能」「リスクが低い」「再現可能」な性質。

### Tier 2 — 一括承認（"全部OK" か "番号で却下"）

複数の修正をまとめて一度に承認/却下する。個別の議論はしない：

| 種類 | 条件 |
|------|------|
| 攪乱肢の表現改善 | 誤りの方向を維持しながら表現を磨く。例: 「〇〇のみ」→「〇〇が主軸」 |
| 絶対化表現の排除 | CLAUDE.md ルールに基づく「のみ」「直ちに」「全員」等の置換 |
| 正答位置のシャッフル | 順序意味なしと判定された場合の全体分布均等化 |
| 攪乱肢の長さ調整 | 正答だけ浮く場合の他選択肢の膨らませ／正答の圧縮 |

### Tier 3 — 個別判断（1問ずつ AskUserQuestion）

ユーザーの教育者としての意図が反映されるべき重要な判断：

| 種類 | 内容 |
|------|------|
| 問題文の全面書き換え | シナリオ型への改題、トピックの軸変更など |
| 削除提案 | 重複・国試風味と乖離・修復困難な問題 |
| 重複問題の処理方針 | 統合・棲み分け・片方削除など |
| 正答そのものの変更 | 「正答が実は誤」「2つ選べ化」など答えの構造を変える変更 |
| コアカリコードの大幅変更 | 領域またぎ（D-3→D-2 など）の振り直し |

### 分類のフロー（オーケストレーターの責務）

姉さん（オーケストレーター）は、各係の所見を読んで以下の順序で分類：

1. **複数係が同じ問題を「重大」と指摘** → Tier 3
2. **問題文・正答に手が入る** → Tier 3
3. **問題の構造（選択肢の意味する内容）が変わる** → Tier 2 か Tier 3
4. **表現の磨きや補強のみ** → Tier 2
5. **追記・メタデータ修正のみ** → Tier 1

迷ったら上のTierに上げる（保守的に）。ユーザーは「Tier 2は一括OK」と言いやすいが、「自動反映されてしまった」は取り返しにくい。

## 運用モード

ユーザーとの合意のもと、3つの運用モードを用意している。**現在のデフォルトはモードA**。

### 🟢 モードA（標準・現在のデフォルト）

「次のバッチやって」と言われたら：
1. 対象問題を選定（未レビュー、講義範囲）
2. バックアップ取得＋作業ファイル抽出
3. 5係並列レビュー
4. 3段階分類
5. **Tier 1 を自動反映**（事後レポートで報告）
6. **Tier 2 を一括承認**で確認（AskUserQuestion で「全部OK or 番号で却下」）
7. **Tier 3 を個別判断**（AskUserQuestion で1問ずつ）
8. 承認分を反映、進捗ログ更新、レポート提示

1バッチ（15〜20問）あたり、ユーザーの判断回数は概ね3〜5回。

### 🟡 モードB（高速）

ユーザーが「**モードB**で頼む」と明示した時のみ。Tier 2 も事前承認とみなして自動反映する。

### 🔴 モードC（背景処理／キュー）

ユーザーが「**モードC**で頼む」と明示した時のみ。全バッチを処理してTier 3 案件だけを `_work/queue_lec<N>.md` にまとめ、ユーザーは後で一気に判断する。

## 次バッチの選び方

1. **講義回の確認**: `ai_review_log_第<N>回.md` の存在を確認、未作成なら新規
2. **未処理問題の特定**: 該当lectureで `ai_review` フィールドが付いていない問題から選ぶ
3. **バッチサイズ**: 15〜20問が推奨（時間・トークン・ユーザー集中力のバランス）
4. **テーマのまとまり**: 同分野・近い `new_corecurri_code` でまとめると 5係の効率が上がる
5. **重複候補は同バッチに集める**: F035 / F198 / F872 / F1045 / F1194 のような重複群は、まとめて判断する方が全体像が見える

## セッション開始時の確認事項

新しいセッションで作業を始める時、Claudeは以下を確認する：

- `git status` でローカル変更の有無
- `git pull` の必要性（他マシンで進めた可能性）
- `ai_review_log_第*.md` で進捗状況を把握
- 未コミットの decisions JSON があれば適用済みかチェック

## 注意

- jsonl は直接編集せず、必ずバックアップを取ってから
- AIエージェントは提案のみ。直接書き換えはしない
- 編集後の問題は、必要に応じ admin.html で「✓ 検証済み」化
- 学生フィードバック経由の承認とAIレビュー経由の承認は別ルートだが、最終的に同じ「検証済み」プールに入る
